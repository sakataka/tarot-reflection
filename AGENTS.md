・回答は日本語で行うこと

・Codex は、明示依頼がない限り formatter / format check / import sort の新規導入・設定追加・実行をしない

## パッケージ管理

- パッケージ管理・実行コマンドは原則 Bun を使う。
- npm / npx は明示的な理由がある場合だけ使い、その理由を説明する。
- Vite / React / TypeScript 構成でも `bun install`, `bun run dev`, `bun run build`, `bun test` を優先する。
